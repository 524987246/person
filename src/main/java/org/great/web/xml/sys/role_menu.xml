<?xml version="1.0" encoding="UTF-8" ?>  
<!DOCTYPE mapper PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN" 
"http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd">

<mapper namespace="org.great.web.mapper.sys.RoleMenuMapper">
	<!--基础属性 -->
	<sql id="beanAttr">
		a.id,
		a.role_id,
		a.menu_id,
		a.create_by AS "createBy.id",
		DATE_FORMAT(a.create_date,'%Y-%m-%d') AS "createDate",
		a.update_by AS
		"updateBy.id",
		DATE_FORMAT(a.update_date,'%Y-%m-%d') AS "updateDate",
		a.remarks,
		a.isemploy
	</sql>
	<!--主表名 -->
	<sql id="tableName">
		sys_role_menu
	</sql>
	<!--主表名 基本更新信息 -->
	<sql id="baseUpdateInfo">
		<if test="createBy !=null and createBy.id !=null and createBy.id!=''">
			a.create_by=#{createBy.id},
		</if>
		<if test="createDate !=null and createDate !=''">
			a.create_date=#{createDate},
		</if>
		<if test="updateBy !=null and updateBy.id !=null and updateBy.id!=''">
			a.update_by=#{updateBy.id},
		</if>
		<if test="updateDate !=null and updateDate !=''">
			a.update_date=#{updateDate},
		</if>
	</sql>
	<!--查询条件信息 -->
	<sql id="whereInfo">
		<where>
			a.isemploy=1
			<if test="queryBeginDate != null and queryBeginDate != ''">
				AND a.create_date &gt;= #{queryBeginDate}
			</if>
			<if test="queryEndDate != null and queryEndDate != ''">
				AND a.create_date &lt;= #{queryEndDate}
			</if>
		</where>
	</sql>
	<!-- 查询单个 -->
	<select id="get" parameterType="Long" resultType="org.great.web.bean.sys.RoleMenu">
		SELECT
		<include refid="beanAttr" />
		FROM
		<include refid="tableName" />
		a
		where a.id=#{id}
	</select>
	<!-- 查询集合 -->
	<select id="findList" resultType="org.great.web.bean.sys.RoleMenu"
		parameterType="org.great.web.bean.sys.RoleMenu">
		SELECT
		<include refid="beanAttr" />
		FROM
		<include refid="tableName" />
		a
		<include refid="whereInfo" />
		ORDER BY a.create_date DESC
	</select>
	<!-- 查询总数 -->
	<select id="queryTotal" resultType="Integer"
		parameterType="org.great.web.bean.sys.RoleMenu">
		SELECT
		count(*)
		FROM
		<include refid="tableName" />
		a
		<include refid="whereInfo" />
	</select>
	<!-- 删除 -->
	<select id="delete" parameterType="org.great.web.bean.sys.RoleMenu">
		UPDATE
		<include refid="tableName" />
		SET
		isemploy=#{isemploy}
		WHERE
		id=#{id}
	</select>
	<!-- 删除 根据角色 -->
	<select id="deleteByRoleMenu" parameterType="org.great.web.bean.sys.RoleMenu">
		UPDATE
		<include refid="tableName" />
		SET
		isemploy=#{isemploy}
		<where>
			<if test="roleId !=null and roleId !=''"> and role_id=#{roleId}</if>
			<if test="menuId !=null and menuId !=''"> and menu_id=#{menuId}</if>
		</where>
	</select>
	<!-- 批量删除 -->
	<select id="batchdelete">
		UPDATE
		<include refid="tableName" />
		SET
		isemploy=#{isemploy}
		WHERE
		id in
		<foreach item="id" collection="list" open="(" separator=","
			close=")">
			#{id}
		</foreach>
	</select>
	<!-- 更新 -->
	<select id="update" parameterType="org.great.web.bean.sys.RoleMenu">
		UPDATE
		<include refid="tableName" />
		<set>
			<include refid="baseUpdateInfo" />
			<if test="name !=null and name !=''">name=#{name},</if>
			<if test="roleId !=null and roleId !=''">role_id=#{roleId},</if>
			<if test="menuId !=null and menuId !=''">menu_id=#{menuId},</if>
		</set>
		WHERE
		id=#{id}
	</select>
	<!-- 保存 -->
	<select id="save" parameterType="org.great.web.bean.sys.RoleMenu">
		INSERT INTO
		<include refid="tableName" />
		(create_by,create_date,update_by,
		update_date,role_id,menu_id
		)VALUES(#{createBy.id},#{createDate},#{updateBy.id},
		#{updateDate},#{roleId},#{menuId}
		)
	</select>
</mapper>